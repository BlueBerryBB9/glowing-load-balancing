apiVersion: 1

# Provide sample prometheus dashboard
providers:
  - name: Sample_promotheus_dashboard
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
      refresh_interval: 10s
